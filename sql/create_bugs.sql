CREATE TABLE bugs (
    id INT AUTO_INCREMENT PRIMARY KEY,
    bug_name VARCHAR(255),
    bug_type VARCHAR(255),
    bug_priority VARCHAR(50),
    project_name VARCHAR(255),
    bug_level VARCHAR(50),
    start_date VARCHAR(50)
);
